# Ops Intelligence Safe V2.9 — Final Fix

Correction:
- Remplacement de latestRows() par selectedRows()
- Corrige l’erreur: latestRows is not defined

Important:
Pour ouvrir/tester le logiciel, double-clique sur `index`, pas sur `app`.
- index = le site
- app = le code JavaScript
- styles = le design

Mise à jour:
Dézippe et redrop le dossier complet sur Netlify.
